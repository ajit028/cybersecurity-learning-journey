# Bandit Level 7 → 8

## Goal
Find the password stored in data.txt next to the word "millionth"

## Commands Used
ls
grep millionth data.txt

## Explanation
Used grep to search for a specific word inside the file.

## Key Learning
Efficient searching in large files using grep.

## Real World Use
Log analysis and extracting sensitive data from large datasets.
